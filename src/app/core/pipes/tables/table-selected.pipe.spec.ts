import { TableSelectedPipe } from './table-selected.pipe';

describe('TableSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new TableSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
